import React from 'react';
import {TextField as MUITextField} from "@mui/material";
const TextField = () => {
    return (
        <MUITextField id="outlined-basic" label="Outlined" variant="outlined" />
    );
};

export default TextField;